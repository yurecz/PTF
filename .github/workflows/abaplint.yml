name: ABAP Syntax Check

# Disabled: abaplint doesn't work reliably in GitHub Actions for this repository
# Re-enable when abaplint configuration is fixed
on:
  workflow_dispatch:  # Only allow manual trigger
#  pull_request:
#    paths:
#      - 'src/**/*.abap'
#      - 'src/**/*.clas.xml'
#  push:
#    branches: [main]
#    paths:
#      - 'src/**/*.abap'

jobs:
  abaplint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: abaplint
        uses: abaplint/actions-abaplint@0.4.0
        with:
          config_file: abaplint.json
