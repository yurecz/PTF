name: ABAP Syntax Check

on:
  pull_request:
    paths:
      - 'src/**/*.abap'
      - 'src/**/*.clas.xml'
  push:
    branches: [main]
    paths:
      - 'src/**/*.abap'

jobs:
  abaplint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: abaplint
        uses: abaplint/actions-abaplint@0.4.0
        with:
          config_file: abaplint.json
